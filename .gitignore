# Cargo 构建产物(工作区只有根目录一个 target)
/target/

# 编辑器与系统文件
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.swp
*.sw?

# 环境变量(本地密钥等,不应提交;如需示例请用 .env.example)
.env
.env.*
!.env.example

# 日志与临时文件
logs
*.log
*.tmp

# Trellis 个人工作区(开发者日志、会话记录)只在本机保留;克隆模板后各自 `python ./.trellis/scripts/init_developer.py <name>` 生成
.trellis/workspace/
